'use strict'

const replacers = {
    beforeCompile: [
        {
            key: 'hei',
            replace: 'hello'
        }
    ],
    
    afterCompile: [
        {
            key: 'alpha',
            replace: 'v1'
        }
    ]
}

export default replacers